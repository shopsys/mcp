# shopsys/mcp

Reusable MCP server package for Shopsys Platform projects.

The package will expose the Shopsys database through a restricted MCP HTTP
endpoint with Shopsys-specific authentication, schema exposure rules, and audit
logging.
